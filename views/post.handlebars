{{!-- List of current posts on the Blog Spot website --}}
{{!-- I am expecting this code to render each of the post for the users of the Blog Spot like a home page on a social
media application such as Twitter(X) or Facebook --}}

    {{#each posts}}
<div class="card mb-3">
    <a class="text-decoration-none text-dark fs-5 fw-bolder" href="/posts/{{id}}">
        <div class="card-header">
            {{title}}
        </div>
    </a>
    <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <p class="card-text">{{content}}</p>
        <p class="card-text">Created By: <a class="text-decoration-none text-dark" href="../users/{{user.id}}">{{user.name}}</a> on {{format_date date_created}}</p>
        <a href="#" class="btn btn-primary">Add a Comment</a>
    </div>

</div>
    {{/each}}